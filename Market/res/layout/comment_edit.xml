<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:paddingLeft="@dimen/resource_comment_padding"
    android:paddingRight="@dimen/resource_comment_padding" >

    <RatingBar
        android:id="@+id/comment_ratingbar"
        style="@style/Comment.RatingBar.Edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="@dimen/resource_comment_common_padding"
        android:layout_marginTop="@dimen/resource_comment_common_padding" />

    <EditText
        android:id="@+id/comment_edit"
        android:layout_width="fill_parent"
        android:layout_height="0.0dip"
        android:layout_weight="1.0"
        android:gravity="left"
        android:padding="10dip"
        android:hint="@string/resource_comment_edit_text_default"
        android:maxLength="300" />

    <Button
        android:id="@+id/comment_publish"
        android:layout_width="180dip"
		android:layout_height="44dip"
        android:background="@drawable/button_bg"
        android:textColor="@color/white"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="@dimen/resource_comment_common_padding"
        android:layout_marginTop="@dimen/resource_comment_common_padding"
        android:text="@string/resource_comment_publish" />

</LinearLayout>